generalize some of my styles so I can apply them to other pages
fix some responsitivity issues and ensure mobile compatibility
include the login menu in the header at the top of the layout
don't route the user to /profile, but instead to /profile/{{name}}
combine authentication credentials into a single module